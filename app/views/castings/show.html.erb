<div class="page-header">
  <h3>Casting #<%= @casting.id %></h3>
</div>

<div class="row">
  <div class="col-md-12 mb-2">
    <dl class="dl-horizontal">
      <dt>Movies</dt>
      <dd>
        <% if @casting.movies.present? %>
          <a href="/movies/<%= @casting.movies_id %>">
            <%= @casting.movies.title %>
          </a>
        <% end %>
      </dd>

      <dt>Actors</dt>
      <dd>
        <% if @casting.actors.present? %>
          <a href="/actors/<%= @casting.actors_id %>">
            <%= @casting.actors.name %>
          </a>
        <% end %>
      </dd>

      <dt>Character name</dt>
      <dd><%= @casting.character_name %></dd>

    </dl>

    <div class="btn-group btn-group-justified">
      <a href="/castings" class="btn btn-primary">
        Back
      </a>
      <a href="/castings/<%= @casting.id %>/edit" class="btn btn-warning">
        Edit
      </a>
      <a href="/delete_casting/<%= @casting.id %>" class="btn btn-danger" rel="nofollow">
        Delete
      </a>
    </div>
  </div>
</div>
